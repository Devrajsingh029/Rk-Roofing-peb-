{
  "name": "rk-roofing-peb",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "vite build && node copy-static.mjs",
    "build:dev": "vite build --mode development && node copy-static.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 8080",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "wrangler": "^4.20.0"
  }
}
